# 🔐 Authentication Flow Diagram

## Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER VISITS ANY PAGE                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      MIDDLEWARE RUNS                            │
│  • Checks for 'invoice_token' cookie                           │
│  • Verifies JWT token                                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              ✅ AUTHENTICATED      ❌ NOT AUTHENTICATED
                    │                   │
                    ↓                   ↓
        ┌───────────────────┐   ┌──────────────────┐
        │ Visiting /login?  │   │ Visiting /login? │
        └───────────────────┘   └──────────────────┘
                │                       │
        ┌───────┴───────┐       ┌──────┴──────┐
        │               │       │             │
       YES             NO      YES           NO
        │               │       │             │
        ↓               ↓       ↓             ↓
   Redirect to /   Allow access  Show login  Redirect to
                                   page      /login?from=<url>
```

---

## Login Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER ON LOGIN PAGE                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              CHECK IF ALREADY AUTHENTICATED                     │
│  useEffect(() => fetch('/api/auth/me'))                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              ✅ AUTHENTICATED      ❌ NOT AUTHENTICATED
                    │                   │
                    ↓                   ↓
            Redirect to /         Show login form
                                        ↓
                              User enters credentials
                                        ↓
                              Submit form (POST)
                                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                  POST /api/auth/login                           │
│  • Validate email/password                                      │
│  • Hash password and compare                                    │
│  • Generate JWT token                                           │
│  • Set HTTP-only cookie                                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              ✅ SUCCESS            ❌ FAILED
                    │                   │
                    ↓                   ↓
        Return { token, user }    Return 401 error
                    ↓                   ↓
        Cookie set automatically   Show error toast
                    ↓
        router.replace(from || '/')
                    ↓
        router.refresh() // Force middleware
                    ↓
        Middleware allows access
                    ↓
        User sees home page
```

---

## Protected Page Access Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              USER VISITS PROTECTED PAGE (e.g., /)               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE CHECK                             │
│  • Checks cookie                                                │
│  • Verifies JWT                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              ✅ VALID TOKEN        ❌ NO/INVALID TOKEN
                    │                   │
                    ↓                   ↓
            Allow request         Redirect to /login?from=/
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PAGE COMPONENT LOADS                         │
│  • Shows loading spinner                                        │
│  • useEffect runs                                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              CLIENT-SIDE AUTH CHECK                             │
│  fetch('/api/auth/me')                                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              ✅ AUTHENTICATED      ❌ NOT AUTHENTICATED
                    │                   │
                    ↓                   ↓
        setIsAuthenticated(true)  router.replace('/login')
                    ↓
        Fetch page data (companies, services, etc.)
                    ↓
        Render page content
```

---

## Logout Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                  USER CLICKS LOGOUT BUTTON                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                Clear cookie client-side                         │
│  document.cookie = 'invoice_token=; Max-Age=0'                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  router.push('/login')                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        User redirected to login page
                              ↓
        Middleware blocks access to protected routes
```

---

## Cookie & JWT Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      LOGIN SUCCESS                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  SERVER GENERATES JWT                           │
│  jwt.sign({ userId, role }, SECRET, { expiresIn: '7d' })      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  SET HTTP-ONLY COOKIE                           │
│  response.cookies.set('invoice_token', token, {                │
│    httpOnly: true,    // Not accessible via JS                 │
│    secure: true,      // HTTPS only (production)               │
│    sameSite: 'lax',   // CSRF protection                       │
│    maxAge: 604800,    // 7 days                                │
│  })                                                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              COOKIE SENT WITH EVERY REQUEST                     │
│  Browser automatically includes cookie in all requests          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              MIDDLEWARE VERIFIES ON EACH REQUEST                │
│  const token = req.cookies.get('invoice_token')                │
│  const payload = jwt.verify(token, SECRET)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## File Structure

```
src/
├── middleware.ts              # 🛡️ Route protection & redirects
├── lib/
│   └── auth.ts               # 🔐 JWT utilities (sign, verify)
├── app/
│   ├── login/
│   │   └── page.tsx          # 🔑 Login form & auth logic
│   ├── page.tsx              # 🏠 Protected home page
│   ├── admin/
│   │   └── page.tsx          # 🔒 Protected admin page
│   └── api/
│       └── auth/
│           ├── login/
│           │   └── route.ts  # 📝 Login API endpoint
│           ├── logout/
│           │   └── route.ts  # 🚪 Logout API endpoint
│           └── me/
│               └── route.ts  # 👤 Get current user
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      LAYER 1: MIDDLEWARE                        │
│  • Runs on every request                                        │
│  • Checks JWT token in cookie                                   │
│  • Redirects unauthenticated users                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  LAYER 2: CLIENT-SIDE CHECK                     │
│  • useEffect in page components                                 │
│  • Calls /api/auth/me                                          │
│  • Shows loading state                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  LAYER 3: API ROUTE PROTECTION                  │
│  • Each API route can check auth                                │
│  • Verify token from cookie                                     │
│  • Return 401 if unauthorized                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Quick Reference

| Action | Endpoint | Method | Cookie Set? |
|--------|----------|--------|-------------|
| Login | `/api/auth/login` | POST | ✅ Yes |
| Logout | `/api/auth/logout` | POST | ❌ Cleared |
| Check Auth | `/api/auth/me` | GET | - |
| Protected Page | `/`, `/admin`, etc. | GET | - |

| Route | Authenticated | Not Authenticated |
|-------|---------------|-------------------|
| `/login` | Redirect to `/` | Show login form |
| `/` | Show page | Redirect to `/login` |
| `/admin` | Show page | Redirect to `/login?from=/admin` |
| `/api/*` | Allow | Allow (except auth endpoints) |

---

## Test Scenarios

✅ **Scenario 1:** Visit home page without login
- Expected: Redirect to `/login?from=/`

✅ **Scenario 2:** Login with valid credentials
- Expected: Redirect to `/` (or original page)

✅ **Scenario 3:** Visit login page when already logged in
- Expected: Redirect to `/`

✅ **Scenario 4:** Logout and try to access protected page
- Expected: Redirect to `/login`

✅ **Scenario 5:** Token expires
- Expected: Redirect to `/login` on next request
